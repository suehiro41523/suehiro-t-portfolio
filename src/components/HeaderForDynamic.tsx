import { Header } from './Header';
import { Box, Link } from '@chakra-ui/react';

export const HeaderForDynamic = () => {
  return (
    <Header>
      <Link href="/">Back to top</Link>
    </Header>
  );
};
